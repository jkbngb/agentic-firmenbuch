# secrets (real .env files are ignored; *.example templates are tracked)
.env
.env.*
!*.example

# local agent/editor tooling
.claude/

# python
__pycache__/
*.py[cod]
.venv/
.uv/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage

# os / editor
.DS_Store
.idea/
.vscode/

# local data / scratch (never commit real Firmenbuch data dumps)
data/
# …but the bundled classification taxonomy/crosswalk IS source, not a data dump:
!packages/core/src/fbl_core/classification/data/
!packages/core/src/fbl_core/classification/data/**
*.local.json

# bicep build artifacts
infra/main.json

# agent build-context, not for the public repo (kept locally)
CLAUDE.md
.grind/
